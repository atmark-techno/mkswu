# mkswu

the main branch was renamed master and can be found here:

https://github.com/atmark-techno/mkswu

Or for a git checkout (from github's help message):

```
git branch -m main master
git fetch origin
git branch -u origin/master master
git remote set-head origin -a
```

#!/bin/sh

# note: will not persist on future base_os upgrades
# so you will need to run that script again everytime.

# see scripts/pre_rootfs.sh copy_to_target section for files
# that are made persistent

date > /root/custom_script
